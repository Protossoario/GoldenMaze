using UnityEngine;
using System.Collections;

public class EnemyScript : MonoBehaviour {
	public bool agressive;// indica si el enemigo esta haciendo un esfuerzo por perseguir al jugador, o si esta en estado de ocio
	public bool turn;
	public int vision;
	public int direction;
	// Use this for initialization
	void Start () {
	
	}
		//funcion a llamar para cuando es el turno del enemigo.
	void run(){

	}
	// Update is called once per frame
	void Update () {
	
	}
}
